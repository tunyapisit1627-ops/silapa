@model IEnumerable<news>

@{
    ViewData["Title"] = "ข่าวประชาสัมพันธ์ทั้งหมด";
    // ใช้ Layout หน้าบ้าน (Frontend) ที่ไม่มีเมนู Admin
    // เช่น Layout = "~/Views/Shared/_Layout.cshtml"; 
    
    int currentPage = ViewBag.CurrentPage ?? 1;
    int totalPages = ViewBag.TotalPages ?? 1;
}

<section class="container">
    <div class="section-title">
        <h2>📰 ข่าวสารและกิจกรรมทั้งหมด</h2>
        <p>หน้าแสดงข่าวประชาสัมพันธ์และกิจกรรมที่เผยแพร่ทั้งหมด</p>
    </div>

    <div class="news-grid">
        @if (Model != null && Model.Any())
        {
            @foreach (var item in Model)
            {
                <div class="news-card">
                    
                    @* 🚨 Badge: ใช้ BadgeText และ BadgeClass *@
                    @if (!string.IsNullOrEmpty(item.BadgeText))
                    {
                        <div class="news-badge @item.BadgeClass">
                            @item.BadgeText
                        </div>
                    }

                    @* 🚨 Image/Background: ใช้ CoverImageUrl หรือ BackgroundGradient *@
                    <div class="news-image" 
                         style="background: @(string.IsNullOrEmpty(item.CoverImageUrl) ? item.BackgroundGradient : $"url(/{item.CoverImageUrl}) no-repeat center center / cover;");">
                    </div>
                    
                    <div class="news-content">
                        <div class="news-meta">
                            <span class="news-date">📅 @item.lastupdate?.ToString("d MMM yyyy", new System.Globalization.CultureInfo("th-TH"))</span>
                            <span class="news-category">@item.Category</span>
                        </div>
                        
                        <h3>@item.titlename</h3>
                        
                        @* 🚨 Details: ตัดข้อความให้สั้นลง *@
                        <p>
                            @{
                                string cleanDetails = System.Text.RegularExpressions.Regex.Replace(item.details ?? "", "<.*?>", String.Empty);
                                string summary = cleanDetails.Length > 120 ? cleanDetails.Substring(0, 120) + "..." : cleanDetails;
                            }
                            @summary
                        </p>
                        
                        @* ลิงก์ไปยังหน้าแสดงรายละเอียดข่าว *@
                        <a asp-controller="News" asp-action="Details" asp-route-id="@item.id" class="news-link">อ่านต่อ →</a>
                    </div>
                </div>
            }
        }
        else
        {
            <div class="col-12 text-center py-5">
                <p>⚠️ ยังไม่มีข่าวประชาสัมพันธ์ในขณะนี้</p>
            </div>
        }
    </div>

    @* 🚨 ส่วน Pagination (เลขหน้า) *@
    @if (totalPages > 1)
    {
        <nav aria-label="Page navigation" style="margin-top: 30px; text-align: center;">
            <ul class="pagination justify-content-center">
                
                @* ปุ่ม Previous *@
                <li class="page-item @(currentPage <= 1 ? "disabled" : "")">
                    <a class="page-link" asp-action="frmList" asp-route-page="@(currentPage - 1)">Previous</a>
                </li>

                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(i == currentPage ? "active" : "")">
                        <a class="page-link" asp-action="frmList" asp-route-page="@i">@i</a>
                    </li>
                }

                @* ปุ่ม Next *@
                <li class="page-item @(currentPage >= totalPages ? "disabled" : "")">
                    <a class="page-link" asp-action="frmList" asp-route-page="@(currentPage + 1)">Next</a>
                </li>
            </ul>
        </nav>
    }
</section>